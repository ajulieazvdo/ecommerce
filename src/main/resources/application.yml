server:
  servlet:
    context-path: /ecommerce/api

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce?useTimezone=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: false


